libnetconf 0.8.0

This release includes the following changes:

 * NETCONF Call Home implementation
 * TLS transport support (experimental)
 * TransAPI moved to version 4 (allow TransAPI module to change order
   of the callback execution)
 * Many bugfixes in :with-defaults capability
 * Support for standalone transAPI modules for augmenting data models
 * New functions nc_datetime2time() and nc_time2datetime() to handle
   NETCONF date and time format (RFC 3339)
 * Improved support for multithreading applications
 * Improved support for embedded systems

For API/ABI compatibility to previous versions, see reports in
'doc/compat_reports/' directory.

